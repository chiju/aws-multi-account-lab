apiVersion: v1
kind: Service
metadata:
  name: opencost
  namespace: opencost
  labels:
    app: opencost
spec:
  selector:
    app: opencost
  ports:
  - name: http
    port: 9003
    targetPort: 9003
  - name: http-ui
    port: 9090
    targetPort: 9090

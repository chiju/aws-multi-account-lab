apiVersion: v1
kind: Service
metadata:
  name: {{ include "x-clone.fullname" . }}
  labels:
    {{- include "x-clone.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "x-clone.selectorLabels" . | nindent 4 }}
